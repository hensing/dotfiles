# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 10
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# solarized colorscheme:
set $back15    #002b36
set $back20    #073642
set $back45    #586e75
set $back50    #657b83
set $back60    #839496
set $back65    #93a1a1
set $back92    #eee8d5
set $back97    #fdf6e3
set $yellow    #b58900
set $orange    #cb4b16
set $red       #dc322f
set $magenta   #d33682
set $violet    #6c71c4
set $blue      #268bd2
set $cyan      #2aa198
set $green     #859900

# class                 border  backgr. text    indicator
#client.focused          $back65 $back60 $back97 $blue
#client.focused_inactive $back50 $back45 $back92 $back65
#client.unfocused        $back20 $back20 $back65 $back60
#client.urgent           $orange $red    $back97 $yellow

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window:
# mod+shift+q
bind $mod+Shift+24 kill

# start dmenu (a program launcher)
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# mod+d
bind $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
# mod+j, mod+k, mod+l, mod+ö
bind $mod+44 focus left
bind $mod+45 focus down
bind $mod+46 focus up
bind $mod+47 focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bind $mod+Shift+44 move left
bind $mod+Shift+45 move down
bind $mod+Shift+46 move up
bind $mod+Shift+47 move right

# switch move to output
# mod+ä, mod+Shift+ä (y on neo)
bind $mod+48 focus output right
bind $mod+Shift+48 move to output right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation:
# mod+h
bind $mod+43 split h

# split in vertical orientation:
# mod+v
bind $mod+55 split v

# enter fullscreen mode for the focused container:
# mod+f
bind $mod+41 fullscreen

# change container layout (stacked, tabbed, toggle split)
# mod+s
bind $mod+39 layout stacking
# mod+w
bind $mod+25 layout tabbed
# mod+e
bind $mod+26 layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# mod+a
bind $mod+38 focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bind $mod+10 workspace number 1
bind $mod+11 workspace number 2
bind $mod+12 workspace number 3
bind $mod+13 workspace number 4
bind $mod+14 workspace number 5
bind $mod+15 workspace number 6
bind $mod+16 workspace number 7
bind $mod+17 workspace number 8
bind $mod+18 workspace number 9
bind $mod+19 workspace number 10
bind $mod+20 workspace number 11
bind $mod+21 workspace number 12

# move focused container to workspace
bind $mod+Shift+10 move container to workspace number 1
bind $mod+Shift+11 move container to workspace number 2
bind $mod+Shift+12 move container to workspace number 3
bind $mod+Shift+13 move container to workspace number 4
bind $mod+Shift+14 move container to workspace number 5
bind $mod+Shift+15 move container to workspace number 6
bind $mod+Shift+16 move container to workspace number 7
bind $mod+Shift+17 move container to workspace number 8
bind $mod+Shift+18 move container to workspace number 9
bind $mod+Shift+19 move container to workspace number 10
bind $mod+Shift+20 move container to workspace number 11
bind $mod+Shift+21 move container to workspace number 12

# reload the configuration file
# mod+shift+C
bind $mod+Shift+54 reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# mod+Shift+R
bind $mod+Shift+27 restart
# exit i3 (logs you out of your X session)
# mod+Shift+E
bind $mod+Shift+26 exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bind 44 resize shrink width 10 px or 10 ppt
        bind 45 resize grow height 10 px or 10 ppt
        bind 46 resize shrink height 10 px or 10 ppt
        bind 47 resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# resize window
# mod+r
bind $mod+27 mode "resize"


# mark / goto mark
bindsym $mod+Shift+M exec i3-input -F 'mark %s' -l 1 -P 'Mark: '
bindsym $mod+M exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Go to: '


# Workspaces zuweisen:
assign [class="Spotify"         ] → 10
assign [class="Pidgin"          ] → 9
assign [class="Skype"           ] → 9
assign [class="Firefox"         ] → 8
assign [instance="Navigator"    ] → 8
assign [instance="Mail"         ] → 7
assign [class="Thunderbird"     ] → 7
assign [instance="bitcoin-qt"   ] → 6
assign [instance="Steam"        ] → 5

# assign floats
#for_window [instance="Steam"         ] #floating enable border none
#for_window [instance="nm-applet"     ] #border none
#for_window [instance="skype"         ] floating enable border none

# Make the currently focused window a scratchpad
#bindsym $mod+Shift+C move scratchpad

# Show the first scratchpad window
#bindsym $mod+c scratchpad show

# bind shift + volume to next/prev song
bindsym Shift+XF86AudioRaiseVolume exec dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Next
bindsym Shift+XF86AudioLowerVolume exec dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Previous


# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +10% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -- -10% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Sreen brightness controls
bindsym XF86MonBrightnessUp   exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Touchpad controls
bindsym XF86TouchpadToggle exec ~/bin/toggletouchpad.sh # toggle touchpad

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    colors {
        background $back15
        statusline $back92

        # bereich            #border   #background #text
        focused_workspace    $back65   $back45     $back97
        active_workspace     $back65   $back45     $back92
        inactive_workspace   $back20   $back15     $back65
        urgent_workspace     $yellow   $orange     $back97
    }

    #status_command ~/bin/my_i3status.sh
    status_command i3status
    #position bottom
    position top
}

# autostart:
exec unity-settings-daemon
exec gnome-screensaver
exec nm-applet
#exec gnome-sound-applet
exec pasystray || gnome-sound-applet
exec /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1

## set desktop wallpaper using feh
exec_always --no-startup-id feh --bg-scale ~/Pictures/current

## Lock screen, start time for automatic screen lock
#bindsym $mod+Ctrl+L exec i3lock -i ~/Pictures/lockscreen.png
#exec_always xautolock -time 10 -locker 'i3lock -i ~/Pictures/lockscreen.png'
bind $mod+Ctrl+46 exec gnome-screensaver-command -l

# Startup programs
exec dropbox start
exec ~/bin/syncthing -no-browser
exec owncloud
exec redshift -l 50.4:7.6 -t 6200:5300
#exec /usr/libexec/deja-dup/deja-dup-monitor
